# Proxy Examples and Multi-language UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add reverse-proxy HTTPS examples and web UI language switching.

**Architecture:** Keep the plugin HTTP-only. Add a small inline i18n dictionary and translation helpers to the existing single HTML UI.

**Tech Stack:** Java resource HTML/CSS/JS, Maven, Markdown.

---

### Task 1: README Reverse Proxy Docs

**Files:**
- Modify: `README.md`

- [ ] Add `Reverse Proxy / HTTPS` after `First Setup`.
- [ ] Include Nginx, Caddy, and Cloudflare Tunnel examples targeting `http://127.0.0.1:4242`.
- [ ] Recommend `web.bind-address: "127.0.0.1"` and `security.secure-cookies: true` when external access is HTTPS.
- [ ] Remove implemented items from `Useful Future Features`.

### Task 2: Web UI Translations

**Files:**
- Modify: `src/main/resources/webconsole.html`

- [ ] Add language selector styles compatible with mobile header/login.
- [ ] Add selectors to login and app header.
- [ ] Add `I18N`, `currentLang`, `t()`, `setLanguage()`, and `applyTranslations()` helpers.
- [ ] Mark static UI strings with `data-i18n`, placeholders with `data-i18n-placeholder`, and titles with `data-i18n-title`.
- [ ] Replace JS-generated UI/toast/status strings with `t(key)`.
- [ ] Keep logs, Minecraft output, player messages, and command text untranslated.

### Task 3: Verification

**Files:**
- Verify only.

- [ ] Run `mvn clean package`.
- [ ] Confirm both jars still exist in `target/`.
- [ ] Commit docs and UI changes with Conventional Commits.
